-- 관리자 계정 테이블
create table admin(
	 admin_id int primary key auto_increment
	, host_id varchar(20)
	, password varchar(20)
	, name varchar(20)
	, email varchar(50)
) default character set utf8;

insert into admin(host_id, password, name, email) values('xodud','1234','엄태영','xodud2972@naver.com');

s